PKG=sentinix-perlmodules

include ../sxarchconfig
include sxconfig

$(PKG): compile install

compile:
	@echo "sorry, no \"compile\", try \"make\" or \"make install\" instead."

install:
	./install.sh

clean:
	./clean.sh

version:
	@echo $(PKG)-$(VERSION)-$(SXARCH)-$(BUILD)
