#!/bin/sh

cd misc &&

# install the mksnortcentersensorcert script
( cat mksnortcentersensorcert > /usr/local/sbin/mksnortcentersensorcert &&
  chown root.root /usr/local/sbin/mksnortcentersensorcert &&
  chmod 0755 /usr/local/sbin/mksnortcentersensorcert )

